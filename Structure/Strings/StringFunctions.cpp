#include<bits/stdc++.h>
using namespace std;


int main() {
    // string s = "abcd";
    // cout<<s.size()<<endl;
    // cout<<s.length()<<endl;
    // s.push_back('e');
    // s.push_back('f');
    // s.push_back('g');
    // cout<<s<<endl;
    // s.pop_back();
    // cout<<s<<endl;

    // string s = "abc";
    // string t = "def";
    // s = s+t+'0';
    // cout<<s;

    // string s = "abcdef";
    // int x = s.size()/2;
    // reverse(s.begin(),s.end()-x);
    // cout<<s;

    // string s = "abcdef";
    // // s.substr(index,length);
    // int x = s.size()/2;
    // cout<<s.substr(x);

    // int x = 1234;
    // string s = to_string(x);
    // cout<<s;

   

    string s = "1234";
    int x = stoi(s);
    cout<<x;

    return 0;
}
