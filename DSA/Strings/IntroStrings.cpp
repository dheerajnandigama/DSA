#include<bits/stdc++.h>
using namespace std;


int main() {
    string s;
    // cin>>s; // only if the given string has no spaces
    getline(cin,s); // if the string has spaces
    cout<<s;

    
    return 0;
}
